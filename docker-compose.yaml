version: '3.8'  # Versión de Docker Compose

services:
  db:
    image: postgres:latest  
    container_name: SpotifyDB  
    environment:
      POSTGRES_USER: DaveR      
      POSTGRES_PASSWORD: password  
      POSTGRES_DB: 50spotifyDB    
    ports:
      - "5432:5432"  
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
